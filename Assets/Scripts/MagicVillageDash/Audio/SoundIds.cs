namespace MagicVillageDash.Audio
{
    public enum MusicId   { GameTheme, GameTheme2 }
    public enum SfxId     { Coin, Swipe, Jump, Slide, Hit }
    public enum UIId      { Click, Confirm, Back }
    public enum AmbientId { Wind, Market, River }
    public enum VoiceId   { None } // add if you’ll use voice
}